<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">

  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>org.miravm</groupId>
    <artifactId>miravm-dist-parent</artifactId>
    <version>1.0.0</version>
  </parent>

  <artifactId>miravm-dist</artifactId>
  <name>MiraVM Distribution (tar.gz package)</name>
  <packaging>pom</packaging>

  <properties>
    <miravmdir>${basedir}/../..</miravmdir>
    <miravm.version>${project.version}</miravm.version>
  </properties>

  <profiles>
    <profile>
      <id>ojdk8</id>
      <dependencies>
        <dependency>
          <groupId>org.miravm</groupId>
          <artifactId>miravm-rt-ojdk8</artifactId>
          <version>${miravm.version}</version>
          <exclusions>
            <exclusion>
              <groupId>org.miravm</groupId>
              <artifactId>miravm-rt-common</artifactId>
            </exclusion>
          </exclusions>
        </dependency>
        <dependency>
          <groupId>org.miravm</groupId>
          <artifactId>miravm-rt-ojdk8</artifactId>
          <version>${miravm.version}</version>
          <classifier>sources</classifier>
          <exclusions>
            <exclusion>
              <groupId>org.miravm</groupId>
              <artifactId>miravm-rt-common</artifactId>
            </exclusion>
          </exclusions>
        </dependency>
      </dependencies>
    </profile>
  </profiles>

  <dependencies>
    <dependency>
      <groupId>org.miravm</groupId>
      <artifactId>miravm-dist-compiler</artifactId>
      <version>${miravm.version}</version>
    </dependency>
    <dependency>
      <groupId>org.miravm</groupId>
      <artifactId>miravm-rt</artifactId>
      <version>${miravm.version}</version>
      <exclusions>
        <exclusion>
          <groupId>org.miravm</groupId>
          <artifactId>miravm-rt-common</artifactId>
        </exclusion>
      </exclusions> 
    </dependency>
    <dependency>
      <groupId>org.miravm</groupId>
      <artifactId>miravm-rt</artifactId>
      <version>${miravm.version}</version>
      <classifier>sources</classifier>
      <exclusions>
        <exclusion>
          <groupId>org.miravm</groupId>
          <artifactId>miravm-rt-common</artifactId>
        </exclusion>
      </exclusions>
    </dependency>    
  </dependencies>

  <build>
    <plugins>
      <plugin>
        <artifactId>maven-antrun-plugin</artifactId>
        <version>1.7</version>
        <executions>
           <execution>
            <id>create-license-txt</id>
            <phase>prepare-package</phase>
          </execution>
        </executions>
      </plugin>
      <plugin>
        <groupId>org.codehaus.mojo</groupId>
        <artifactId>exec-maven-plugin</artifactId>
        <version>1.3.2</version> 
        <executions>
          <execution>
            <id>fetch-vm-binaries</id>
            <phase>compile</phase>
            <goals>
              <goal>exec</goal>
            </goals>
            <configuration>
              <executable>${robovmdir}/vm/fetch-binaries.sh</executable>
              <skip>${skipFetchBinaries}</skip>
            </configuration>
          </execution>
        </executions>
      </plugin>
      <plugin>
        <artifactId>maven-assembly-plugin</artifactId>
        <version>2.4</version>
        <executions>
          <execution>
            <id>distro-full-assembly</id>
            <phase>package</phase>
            <goals>
              <goal>single</goal>
            </goals>
            <configuration>
              <descriptors>
                <descriptor>src/main/assembly/dist-full.xml</descriptor>
              </descriptors>
              <finalName>miravm-${project.version}</finalName>
              <appendAssemblyId>false</appendAssemblyId>
            </configuration>
          </execution>
          <execution>
            <id>distro-nocompiler-assembly</id>
            <phase>package</phase>
            <goals>
              <goal>single</goal>
            </goals>
            <configuration>
              <descriptors>
                <descriptor>src/main/assembly/dist-nocompiler.xml</descriptor>
              </descriptors>
              <finalName>miravm-${project.version}</finalName>
              <appendAssemblyId>true</appendAssemblyId>
            </configuration>
          </execution>
        </executions>
      </plugin>
    </plugins>
  </build>

  <version>1.0.0</version>
</project>

